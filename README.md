# Randomizer

A simple Python console application that prints a random line from a `.txt` file.

## How it works

The program takes the path to a text file as an argument, reads all non-empty lines, and prints one random line.

## Usage

### Run with Python

```bash
python randomizer.py names.txt
